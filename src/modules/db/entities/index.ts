import { ActiveStepEntity } from './active-step.entity';
import { ChatSessionEntity } from './chat-session.entity';
import { GameTaskEntity } from './game-task.entity';

export const entities = [ChatSessionEntity, ActiveStepEntity, GameTaskEntity];
